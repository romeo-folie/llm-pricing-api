{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "GOBIN=$(pwd)/bin go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest && go build -o bin/api ./cmd/api && go build -o bin/worker ./cmd/worker",
    "watchPatterns": [
      "**/*.go",
      "go.mod",
      "go.sum",
      "railway.json",
      "start.sh",
      "migrations/**"
    ]
  },
  "deploy": {
    "startCommand": "./start.sh",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
